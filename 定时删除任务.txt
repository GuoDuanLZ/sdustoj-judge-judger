此文档介绍如何配置评测机定时删除用户提交的数据
1.打开控制台输入crontab -e
2.在打开的文档中末尾添加如下命令：
* */1 * * * find path/* -cmin +30 -exec rm -rf {} \;

说明：
"* */1 * * *"中，"*/1"的意思为每小时执行一次
"path/*"中，将path替换成需要删除的数据所在目录，例如: "/home/judge/data/*"
"-cmin +30"的意思为将操作三十分钟之前的数据
"-exec rm -rf" 删除命令
 

